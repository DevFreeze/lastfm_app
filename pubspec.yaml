name: lastfm_app
description: Coding challenge with LastFM API.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: 1.0.2
  easy_localization: 3.0.0
  flutter_dotenv: 5.0.2
  get_it: 7.2.0
  http: 0.13.0
  provider: 6.0.1
  url_launcher: 6.0.10

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/intl/
    - .env
